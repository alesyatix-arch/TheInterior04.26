
## План внедрения правок из ТЗ

Работу разобью на 4 этапа. После каждого — проверка в превью.

### Этап 1. Первый экран + блок Кэш-бек

**Hero.tsx:**
- Подзаголовок: «Вы получаете планировку, визуализации или полный дизайн-проект на основе готовых авторских интерьерных решений, адаптированных под вашу квартиру».
- Заголовок: «Дизайн-проект квартиры для ремонта в 2 раза выгоднее и в 3 раза быстрее».
- 7 преимуществ (от 900 ₽/м², срок 7–21, 3 формата, готовые решения, комплектация, переход к реализации, до 100% кэш-бек).
- Убрать вторую кнопку «Выбрать формат», оставить только «Рассчитать стоимость проекта».
- Плашку кэш-бека сделать максимально сдержанной.

**Новый компонент `CashbackSection.tsx`** (между Hero и Calculator):
- Заголовок «До 100% кэш-бек на дизайн-проект».
- Текст-описание.
- Схема 1-2-3 (Формат → Готовое решение → Реализация).
- Сноска мелким шрифтом.
- Кнопка «Узнать условия кэш-бека» → `#consultation`.

### Этап 2. Калькулятор, «Почему быстрее», Форматы, Сравнение

**Calculator.tsx:**
- Заголовок «Рассчитайте стоимость проекта за 30 секунд».
- Подпись результата: «При переходе к реализации вы можете получить до 100% стоимости проекта кэш-беком».
- Кнопка «Получить точный расчет».

**WhyLower.tsx:**
- Заголовок «Почему вы получаете проект быстрее и доступнее».
- Текст из ТЗ (3 абзаца).
- Схема-цепочка: Каталог → Адаптация → Планировка → Визуализации → Комплектация → Чертежи → Реализация.
- Важная формулировка-плашка снизу.

**Services/Packages.tsx:**
- Заголовок «Выберите формат проекта под вашу задачу».
- Везде термин «формат проекта» (убрать «пакет/тариф»).

**ComparisonTable.tsx:**
- Заголовок «Чем отличаются форматы проекта».
- Обновить строки таблицы по ТЗ (9 строк).
- Кнопка «Не знаете, что выбрать? Получите консультацию».

### Этап 3. Новые блоки + перестановка

**Новый `ProjectOutcome.tsx`** (после блока Квиз/Examples): «На выходе вы получаете не идею, а понятный проект» — 7 карточек (Планировка, Визуализации, Ведомость, Рабочие планы, Реализация, Авторское сопровождение, Готовый ремонт).

**Новый `FounderWord.tsx`** (после ProjectOutcome): «Почему мы создали этот формат» + фото Артёма Кулагина (взять из `TeamSection`).

**TeamSection.tsx:** добавить цифры — 12 лет студии, 400+ разработанных проектов, 200+ реализованных объектов.

**Новый `FAQ.tsx`** (после команды): 9 вопросов из ТЗ в виде аккордеона.

**Index.tsx:** новый порядок секций:
```
Hero → Cashback → Calculator → WhyLower → Services/Packages → 
ComparisonTable → StylePicker → ProjectExamples → (Quiz) → 
ProjectOutcome → FounderWord → TeamSection → FAQ → 
ConsultationCTA → Footer (с контактами)
```
- Удалить отдельную секцию `Contacts` из середины страницы.

**Footer.tsx:** перенести контакты (телефон, email, Telegram, адрес) в подвал.

### Этап 4. Переименование «Интериор → Твой Интерьер»

Проверить и обновить:
- `PrivacyPolicy.tsx`
- `PublicOffer.tsx`
- `YandexConsent.tsx`
- Любые другие тексты с «Интериор/The Interior» → «Твой Интерьер».
- Сохранить домен `the-interior.ru` (по memory).

### Что НЕ меняется
- StylePicker (выбор стиля) — без изменений.
- ProjectExamples — без изменений.
- Quiz — без изменений (если есть; если нет — пропуск).
- ConsultationCTA / RequestForm — без изменений.
- Дизайн-система (чёрно-белая + красный акцент), типографика, центрирование.

### Технические заметки
- Все новые компоненты — `lazy()` импорт в `Index.tsx`.
- Использовать существующие токены `--foreground`, `--background`, `--primary`.
- Якоря (`#cashback`, `#calculator`, `#packages`, `#faq`, `#consultation`, `#contacts`) обновить в Header-навигации при необходимости.
